{
  "name": "activity-timer-backend",
  "description": "",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "aws-sdk-client-mock": "^2.0.0",
    "esbuild": "^0.19.11"
  },
  "scripts": {
    "start": "sam local start-api",
    "build": "sam build",
    "test": "node --experimental-vm-modules ./node_modules/jest/bin/jest.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.484.0",
    "aws-sdk": "^2.1528.0",
    "axios": "^1.6.3"
  }
}
